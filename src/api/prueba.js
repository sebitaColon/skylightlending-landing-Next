/*import {pool} from "../config/conexion"

export default async function prueba(req, res) {
   const result = await pool.query("SELECT * FROM usuarios")
    
   console.log(result)
    return res.status(200).json({result})
}*/

console.log("estoy aca")